<!-- GENERATED by Norml WordPress Copilot Advanced — DO NOT EDIT BY HAND. Run the architecture rescan to refresh it. -->
# {{THEME_NAME}} — WordPress architecture

- **Project:** `{{SLUG}}`
- **Generated:** {{GENERATED_AT}}
- **Mode:** `{{MODE}}`
- **Production:** {{PROD_URL}}
- **Production WordPress path:** `{{PROD_WP_PATH}}`
- **Production theme path:** `{{PROD_THEME_PATH}}`
- **Source:** read-only WP-CLI over `{{PROD_SSH_ALIAS}}` plus the local theme repo

## At a glance

{{ARCHITECTURE_SUMMARY}}

## Read order

1. [`capabilities.md`](capabilities.md) — verified operating boundary.
2. [`docs/01-infrastructure.md`](docs/01-infrastructure.md) — runtime, URLs,
   versions, GitHub, SSH, and paths.
3. [`docs/02-application.md`](docs/02-application.md) — WordPress core, active
   theme, plugins, and available updates.
4. [`docs/03-theme-architecture.md`](docs/03-theme-architecture.md) — local theme
   stack signals and source inventory.
5. [`docs/04-content-structure.md`](docs/04-content-structure.md) — post types,
   taxonomies, counts, and menus.
6. [`docs/05-issues.md`](docs/05-issues.md) — scan failures, update signals, and
   unresolved setup blockers.

## Rescan

From any terminal with the skill installed:

```bash
bash <skill-folder>/scripts/scrape-architecture.sh {{SLUG}}
```

On Windows PowerShell:

```powershell
& <skill-folder>\scripts\scrape-architecture.ps1 {{SLUG}}
```

Generated files are snapshots. Durable decisions belong in `CLAUDE.md`; durable
work history belongs in `changelog/daily.md`.
